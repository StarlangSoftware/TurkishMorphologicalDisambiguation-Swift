import XCTest
@testable import MorphologicalDisambiguation

final class MorphologicalDisambiguationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MorphologicalDisambiguation().text, "Hello, World!")
    }
}
